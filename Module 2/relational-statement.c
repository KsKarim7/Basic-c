#include <stdio.h>
int main()
{
    // printf("Size of char : %d\n", sizeof(char));
    // return 0;

    int x = 0;
    if (x)
    {
        printf("hi");
    }
    else
    {
        printf("how are u");
    }
}